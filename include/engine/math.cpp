#include "engine/Math.h"

/*
namespace engine {
	template<typename T>
	inline Vector2<T>::Vector2() : x(0), y(0) {}

	template<typename T>
	Vector2<T>::Vector2(const T x, const T y) : x(x), y(y) {}

	template<typename T>
	inline T Vector2<T>::magnitude() const { return std::sqrt(x * x + y * y); }

	template<typename T>
	inline T Vector2<T>::magnitude2() const { return x * x + y * y; }

	template<typename T>
	inline T Vector2<T>::dot(const Vector2<T>& vector) const { return x * vector.x + y * vector.y; }

	template<typename T>
	inline Vector2<T> Vector2<T>::normalize() const { return Vector2<T>(); }

	template<typename T>
	inline Vector2<T>& Vector2<T>::operator=(const T a)
	{
		// TODO: 여기에 return 문을 삽입합니다.
	}
	template<typename T>
	inline Vector2<T>& Vector2<T>::operator+=(const T a)
	{
		// TODO: 여기에 return 문을 삽입합니다.
	}
	template<typename T>
	inline Vector2<T>& Vector2<T>::operator-=(const T a)
	{
		// TODO: 여기에 return 문을 삽입합니다.
	}
	template<typename T>
	inline Vector2<T>& Vector2<T>::operator*=(const T a)
	{
		// TODO: 여기에 return 문을 삽입합니다.
	}
	template<typename T>
	inline Vector2<T>& Vector2<T>::operator/=(const T a)
	{
		// TODO: 여기에 return 문을 삽입합니다.
	}
	template<typename T>
	inline Vector2<T>& Vector2<T>::operator+=(const Vector2<T>& a)
	{
		// TODO: 여기에 return 문을 삽입합니다.
	}
	template<typename T>
	inline Vector2<T>& Vector2<T>::operator-=(const Vector2<T>& a)
	{
		// TODO: 여기에 return 문을 삽입합니다.
	}
	template<typename T>
	inline Vector2<T>& Vector2<T>::operator*=(const Vector2<T>& a)
	{
		// TODO: 여기에 return 문을 삽입합니다.
	}
	template<typename T>
	inline Vector2<T>& Vector2<T>::operator/=(const Vector2<T>& a)
	{
		// TODO: 여기에 return 문을 삽입합니다.
	}
	template<typename T>
	inline Vector2<T> Vector2<T>::operator+(const T a) const
	{
		return Vector2<T>();
	}
	template<typename T>
	inline Vector2<T> Vector2<T>::operator-(const T a) const
	{
		return Vector2<T>();
	}
	template<typename T>
	inline Vector2<T> Vector2<T>::operator*(const T a) const
	{
		return Vector2<T>();
	}
	template<typename T>
	inline Vector2<T> Vector2<T>::operator/(const T a) const
	{
		return Vector2<T>();
	}
	template<typename T>
	inline Vector2<T> Vector2<T>::operator+(const Vector2<T>& a) const
	{
		return Vector2<T>();
	}
	template<typename T>
	inline Vector2<T> Vector2<T>::operator-(const Vector2<T>& a) const
	{
		return Vector2<T>();
	}
	template<typename T>
	inline Vector2<T> Vector2<T>::operator*(const Vector2<T>& a) const
	{
		return Vector2<T>();
	}
	template<typename T>
	inline Vector2<T> Vector2<T>::operator/(const Vector2<T>& a) const
	{
		return Vector2<T>();
	}
}
*/